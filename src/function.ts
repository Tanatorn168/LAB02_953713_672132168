const add = (a:number, b:number):string => {
     const result =a+b;
     return result.toString ();
 }
 export default add; //เรียกใช้งานได้เลย โดยไม่ต้องใช้ {} ในการเรียกใช้งาน

 export const subtract = (a:number, b:number):string => {
    const result =a-b;
    return result.toString ();
}
